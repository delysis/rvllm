# Gemma 4 attention Metal artifact evidence

This directory is a reproducible public-API compiler and device-resource
observation for the sealed L1024 attention source in
`reports/gemma4-attention-matched-20260925-v1/prepared-l1024/candidate.metal`.
It is generated by `rvllm-metal-artifact-evidence` and passes the strict
`rvllm_metal_artifact_evidence_verify` verifier.

Evidence JSON SHA-256:
`ac770ec7584dad8fefe200aae42c01c71d1332cd6b755ec0540193f53f52940c`.

| Pipeline | execution width | max threads | static threadgroup memory |
| --- | ---: | ---: | ---: |
| split-32 partial (`atlas_candidate`) | 32 | 1024 | 2,912 B |
| split-32 merge (`atlas_merge`) | 32 | 1024 | 384 B |
| split-matrix partial | 32 | 1024 | 12,512 B |
| split-matrix merge | 32 | 1024 | 0 B |

The directory retains the exact AIR, metallib, compiler/link logs, public
`metal-objdump` build tables and disassembly used by the receipt. These files
are evidence for compiler inputs and public pipeline properties, not a stable
Apple ISA contract. Public Metal APIs do not expose register count, occupancy,
register residency, SIMD-matrix machine lowering or low-bit unpack arithmetic;
the receipt marks those claims unavailable or unverified instead of inferring
them from unstable textual output.

Recheck from the `v3` directory:

```sh
target/debug/rvllm_metal_artifact_evidence_verify \
  reports/gemma4-metal-artifact-evidence-attention-20260925/evidence.json
```

This artifact makes no correctness, timing, selector or promotion claim.
